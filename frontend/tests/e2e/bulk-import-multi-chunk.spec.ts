import { test } from '@playwright/test'

test.describe('bulk import multi chunk', () => {
  test.skip('multi-chunk happy path placeholder', async () => {
    // Requires full stack and deterministic fixtures in CI/local E2E environment.
  })
})


