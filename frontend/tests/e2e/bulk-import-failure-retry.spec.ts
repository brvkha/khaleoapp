import { test } from '@playwright/test'

test.describe('bulk import failure retry', () => {
  test.skip('failure and retry placeholder', async () => {
    // Requires failure injection hook and seeded auth/session context.
  })
})


